# Experiments

One report per training run, named `<run_id>.md`, plus machine-readable
manifests under `data/manifests/`. See build brief sections 25 and 36.

Every report must answer: What changed? Why? Dataset used? Hardware? Elapsed
time? Result? Did it improve? What failed? What should be tried next?

Negative results are documented, never hidden.
