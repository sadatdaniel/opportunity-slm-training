# Teacher annotation / API credentials. Copy to .env and fill in.
# NEVER commit the real .env file.

# Provider for teacher annotation (provider-configurable by design)
TEACHER_PROVIDER=
TEACHER_BASE_URL=
TEACHER_API_KEY=
TEACHER_MODEL=
