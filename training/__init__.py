"""Per-capability training code. Each capability is independently
trainable/evaluatable/versionable: training/<capability>/{train,evaluate}.py
plus its own config, sharing only the helpers in training/common/.
"""
